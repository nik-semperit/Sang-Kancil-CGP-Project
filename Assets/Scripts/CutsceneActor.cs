using UnityEngine;

public class CutsceneActor : MonoBehaviour
{
    public Animator anim; 
    public bool moveRight = false; // Just a helper variable if needed

    // This ensures other scripts can find "deer.anim" and "deer.transform"
}